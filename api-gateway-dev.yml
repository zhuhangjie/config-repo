zuul:
  routes:
    #/myProduct/product/list -> /product/product/list
    myProduct:
      path: /myProduct/**
      serviceId: product
      sensitiveHeaders:
    ##or anther Method:serviceId:path
    #product: /myProduct/**
    # type is set
  ignored-patterns:
    #- /**/product/list
management:
  security:
    enabled: false
